// Generated by CoffeeScript 1.9.3
(function() {
  var InterpolateProvider, SkillsController;

  InterpolateProvider = (function() {
    function InterpolateProvider($interpolateProvider) {
      $interpolateProvider.startSymbol('<[');
      $interpolateProvider.endSymbol(']>');
    }

    return InterpolateProvider;

  })();

  SkillsController = (function() {
    function SkillsController($scope, $http) {
      $http.get('/files/about/about.json').success(function(data) {
        return $scope.skills = data.skills;
      });
    }

    return SkillsController;

  })();

  angular.module('skillsApp', []).config(['$interpolateProvider', InterpolateProvider]).directive('hoverClass', function() {
    return {
      restrict: 'A',
      scope: {
        hoverClass: '@'
      },
      link: function(scope, element) {
        element.on('mouseenter', function() {
          var maxHeight;
          maxHeight = element.children()[0].offsetHeight;
          angular.element(element.children()[0]).css({
            'transform': "translateY(-" + (maxHeight / 2) + "px) rotateX(90deg)"
          });
          angular.element(element.children()[1]).css({
            'transform': "translateZ(" + (maxHeight / 2) + "px) rotateX(0deg)"
          });
          return element.addClass(scope.hoverClass);
        });
        return element.on('mouseleave', function() {
          var maxHeight;
          maxHeight = element.children()[0].offsetHeight;
          angular.element(element.children()[0]).css({
            'transform': "translateZ(" + (maxHeight / 2) + "px) rotateX(0deg)"
          });
          angular.element(element.children()[1]).css({
            'transform': "translateY(" + (maxHeight / 2) + "px) rotateX(-90deg)"
          });
          return element.removeClass(scope.hoverClass);
        });
      }
    };
  }).controller('skillsController', ['$scope', '$http', SkillsController]);

}).call(this);

//# sourceMappingURL=ng-script.js.map
